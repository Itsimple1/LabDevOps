#---------------------------------------
# GitHub Action LabDevOps

# All rights reserved...  Joke)
#---------------------------------------

# This workflow will install Python dependencies

name: Build->Deploy->Upload

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.11
        uses: actions/setup-python@v2
        with:
          python-version: 3.11
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pytest
        run: pytest --verbosity=2 --showlocals
      
      - name: Build
        run: pyinstaller --onefile main.py
      
      - name: Upload
        uses: actions/upload-artifact@v3
        with:
          name: tg-artifact
          path: ${{ env.app_name }}
          retention-days: 1
  
  send_drive:
    runs-on: ubuntu_latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Archive files
        run: |
          sudo apt-get update
          sudo apt-get install zip
          zip -r arhive.zip *
        
      - name: Upload artifact
        uses: adityak74/google-drive-upload-git-action@main
        with:
          credentials: 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
          filename: "archive.zip"
          folderId: 712399290851-vdlqn3pvilesvmqf6aj1nee0uv86rt32.apps.googleusercontent.com
          name: "archicve.zip"
          overwrite: "true"
      
          
  analys_code:
    name: code analysis
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.11
      uses: actions/setup-python@v2
      with:
          python-version: 3.11
    - name: analys
      run: |
          python -m pip install --upgrade pip
          pip install black
          black --check .
          
  
